<Window x:Class="TestovoeGagarinov.View.CreateWallsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TestovoeGagarinov.View"
             xmlns:vm="clr-namespace:TestovoeGagarinov.ViewModel" d:DataContext="{d:DesignInstance Type=vm:WallsViewModel}"
        mc:Ignorable="d" 
             Title="Построить стены по помещениям"
             WindowStartupLocation="CenterOwner"
             Height="500" Width="400">
    <Grid Margin="20">
        <Grid.RowDefinitions >
            <RowDefinition Height="3*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <ScrollViewer  Grid.Row="0">
            <ListBox ItemsSource="{Binding Rooms}" DisplayMemberPath="Name"/>
        </ScrollViewer>
        <StackPanel Orientation="Vertical" Grid.Row="1" >
            <Button Content="Выбрать помещения" Command="{Binding PickRoomsCommand}" Width="300" Height="30" Margin="0,10,0,0"/>
            <Button Content="Построить стены по граням помещения" Command="{Binding BuildWallsCommand}" Width="300" Height="30" Margin="0,30,0,0"/>
        </StackPanel>
        
    </Grid>
</Window>
